name: pvtro
description: CLI tool for coordinating slang translations across Flutter packages.
version: 0.9.0
repository: https://github.com/Pathverse/pvtro

environment:
  sdk: ^3.0.0

executables:
  pvtro: pvtro

dependencies:
  build: ^4.0.0
  path: ^1.9.0
  yaml: ^3.1.0
  args: ^2.4.2

dev_dependencies:
  build_runner: ^2.5.4
  lints: ^6.0.0
  test: ^1.25.6

