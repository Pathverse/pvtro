import 'package:flutter/widgets.dart';

/// Provides a minimal widget exported by the shared checkout real fixture package.
class SharedCheckoutRealLabel extends StatelessWidget {
  /// Creates the shared checkout real label fixture.
  const SharedCheckoutRealLabel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
