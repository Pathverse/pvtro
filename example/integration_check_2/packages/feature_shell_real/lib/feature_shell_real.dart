import 'package:flutter/widgets.dart';

/// Provides a minimal widget exported by the feature shell real fixture package.
class FeatureShellRealBanner extends StatelessWidget {
  /// Creates the feature shell real banner fixture.
  const FeatureShellRealBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
