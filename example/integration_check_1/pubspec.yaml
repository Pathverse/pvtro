name: integration_check_1
description: Integration fixture for pvtro with nested slang-enabled path dependencies.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  slang: ^4.8.1
  slang_flutter: ^4.8.1
  feature_shell:
    path: packages/feature_shell

dev_dependencies:
  build_runner: ^2.5.4
  flutter_test:
    sdk: flutter
  pvtro:
    path: ../..

flutter:
  uses-material-design: false
