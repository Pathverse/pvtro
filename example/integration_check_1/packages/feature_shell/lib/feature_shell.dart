import 'package:flutter/widgets.dart';

/// Provides a minimal widget exported by the feature shell fixture package.
class FeatureShellBanner extends StatelessWidget {
  /// Creates the feature shell banner fixture.
  const FeatureShellBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
