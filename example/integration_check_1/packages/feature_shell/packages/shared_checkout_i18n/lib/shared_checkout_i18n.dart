import 'package:flutter/widgets.dart';

/// Provides a minimal widget exported by the shared checkout fixture package.
class SharedCheckoutLabel extends StatelessWidget {
  /// Creates the shared checkout label fixture.
  const SharedCheckoutLabel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
